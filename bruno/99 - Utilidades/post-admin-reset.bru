meta {
  name: POST /admin/reset
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/reset
  body: none
  auth: none
}

docs {
  Vuelve los cupos y las compras al inicio (solo el estado de la API). El notificador y el proyector
  viven en otro contenedor y no se resetean desde aqui: usa `docker compose restart notificador proyector`.
}
