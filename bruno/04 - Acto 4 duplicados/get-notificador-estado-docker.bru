meta {
  name: GET notificador /estado (solo Docker, puerto 3001)
  type: http
  seq: 3
}

get {
  url: {{notificadorUrl}}/estado
  body: none
  auth: none
}

docs {
  Solo responde cuando el notificador corre en su propio contenedor (Docker). Sin Docker usa GET /admin/estado.
}
